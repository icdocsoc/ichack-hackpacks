# claude-demo

This project contains a demonstration of how the Claude API can be used as part of a web app.

1. Run `npm install` to install all required libraries and dependencies.

2. Create a `.env` file and paste your API key inside (`ANTHROPIC_API_KEY=sk-...`).

3. Run `npm start` and visit [localhost:3000](localhost:3000) to open the web app.
